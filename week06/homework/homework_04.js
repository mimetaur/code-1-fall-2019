function setup() {
    createCanvas(400, 400);

    background(220);
    noStroke();
    fill(80);
}

function draw() {

    drawSpiral(width / 2, height / 2); // write the function definition for this line
}
