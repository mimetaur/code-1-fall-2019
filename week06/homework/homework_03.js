function setup() {
    createCanvas(400, 400);
}

function draw() {
    background(227);
    rectMode(CENTER);
    fill(0, 120, 180);

    drawGrid();
}

function drawGrid() {
    // outer for (y = ...) {
    // inner for (x = ...) {
    // rect(x,y,30,30);
    // close inner for }
    // close outer }
}